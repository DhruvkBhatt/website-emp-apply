export type Theme = 'executive' | 'warm' | 'playful' | 'intimate' | 'cinematic';

/** §3 — the theme each section runs in. Section.tsx applies it as data-theme. */
export const THEMES: readonly Theme[] = ['executive', 'warm', 'playful', 'intimate', 'cinematic'];
